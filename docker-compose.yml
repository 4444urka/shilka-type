services:
  postgres:
    image: postgres:latest
    container_name: shilka-db
    environment:
      POSTGRES_USER: shilka-type
      POSTGRES_PASSWORD: Churka98765
      POSTGRES_DB: shilka-db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped

volumes:
  postgres_data:
